void main() {
  // String firstName = "omotola";
  // String lastName = "   Temitope  ";
  // var a = firstName;
  // print(a.length);
  // print(a.isEmpty);
  // print(a.isNotEmpty);
  // print(a.runtimeType);
  // print(a.contains("o"));
  // print(a.endsWith("a"));
  // print(a.indexOf(a));
  // print(a.lastIndexOf("a"));
  // print(a.replaceAll("o", "e"));
  // print(a.replaceFirst("o", "a"));
  // print(a.split(''));
  // print(a.substring(4));
  // print(a.toUpperCase());
  // print(a.trim());
  // print(lastName.trimLeft());
  // print(lastName.trimRight());
  // print(a.replaceRange(5, 10, "shade"));

  int age = 3546;
  print(age);

}
